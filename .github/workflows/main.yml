name: CI

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  check-upstream:
    runs-on: ubuntu-latest
    steps:
    - name: vurze check
      run: |
        python -m pip install vurze
        vurze check src/arglite/arglite.py
        vurze check src/arglite/code.py
        vurse check src/arglite/arguments.py
